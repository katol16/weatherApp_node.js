module.exports.add = (a,b) => a+b;

module.exports.square = (x) => x*x;

// Testing with MOCA (Instalcja)
// Instalacja "npm install mocha --save-dev"
// save-dev - zapiszę ten pakiet tylko w wersji developerskiej, tylko dla celów developerskich, wiec warto tego używać